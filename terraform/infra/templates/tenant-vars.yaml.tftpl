# Generated by terraform/infra (local_file) — do not edit manually.
# Populated from Terraform state during `make apply-infra`.

apiVersion: v1
kind: ConfigMap
metadata:
  name: tenant-vars
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  tenant_uuid: "${tenant_uuid}"
  vpc_name: "${vpc_name}"
  argo_namespace: "${argo_namespace}"
