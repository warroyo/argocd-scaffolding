apiVersion: v2
name: bootstrap-tenant
description: "Bootstraps a supervisor namespace — the argo-attach-sa sync identity, plus (deployArgo) the ArgoCD instance, its own ManagedEntity and the root app"
type: application
version: 0.1.0
appVersion: "1.0.0"
