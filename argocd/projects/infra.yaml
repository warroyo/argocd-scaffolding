# Generated by terraform/infra (local_file) — do not edit manually.
# Edit terraform/infra/tenants.yaml and re-run `make apply-infra`.

apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: infra
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  description: infra
  sourceRepos:
  - '*'
  destinations:
  - namespace: '*'
    server: '*'
  clusterResourceWhitelist:
  - group: '*'
    kind: '*'
  namespaceResourceWhitelist:
  - group: '*'
    kind: '*'
  # Platform syncs (ApplicationSets: cluster-provisioning, namespace-resources)
  # fan out across every tenant's own supervisor namespace, each with its own
  # local argo-attach-sa — so this must be the BARE account name (no namespace
  # prefix), which ArgoCD resolves relative to each sync's own destination
  # namespace. A namespace-qualified value only works for a project whose syncs
  # target one fixed namespace (see the tenant branch below). See
  # docs/DECISIONS.md #12.
  destinationServiceAccounts:
  - server: '*'
    namespace: '*'
    defaultServiceAccount: 'argo-attach-sa'
  - server: ''
    namespace: '*'
    defaultServiceAccount: 'argo-attach-sa'
