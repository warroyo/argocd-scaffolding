apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageRepository
metadata:
  name: standard-repo
  namespace: tkg-system
  annotations:
    argocd.argoproj.io/sync-wave: '-2'
spec:
  fetch:
    imgpkgBundle:
      # Placeholder — the env overlay (apps/components/envs/{env}) supplies the
      # real bundle image, so package-repo bumps roll out per environment.
      image: replace-me
