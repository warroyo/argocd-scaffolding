apiVersion: addons.kubernetes.vmware.com/v1alpha1
kind: AddonConfig
metadata:
  name: cluster-ako
  labels:
    app.kubernetes.io/name: ako
    config.k8s.io/inject-cluster-name-prefix: "true"
spec:
  addonConfigDefinitionRef:
     # Placeholder — the env overlay (infrastructure/components/envs/{env})
     # supplies the real addon version, so bumps roll out per environment.
     name: replace-me
     namespace: vmware-system-vks-public
  values:
    AKOSettings:
      istioEnabled: false
