apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: clustername
  labels:
    config.k8s.io/inject-cluster-name: "true"
spec:
  clusterNetwork:
    pods:
      cidrBlocks:
        - 192.168.156.0/20
    services:
      cidrBlocks:
        - 10.96.0.0/12
    serviceDomain: cluster.local
  topology:
    class: builtin-generic-v3.5.0
    version: v1.34.1---vmware.1-vkr.4
    variables:
      - name: vsphereOptions
        value:
          persistentVolumes:
            availableStorageClasses:
              - vsan-default-storage-policy
            defaultStorageClass: vsan-default-storage-policy
      - name: kubernetes
        value:
          certificateRotation:
            enabled: true
            renewalDaysBeforeExpiry: 90
      - name: vmClass
        value: best-effort-xlarge
      - name: storageClass
        value: vsan-default-storage-policy
    controlPlane:
      replicas: 1
      metadata:
        annotations:
          # Placeholder — the env overlay (infrastructure/components/envs/{env})
          # supplies the real resolve-os-image annotation (the content-library ID
          # is environment-specific). A cluster that skips the overlay fails
          # loudly (validate.sh greps rendered output for replace-me).
          run.tanzu.vmware.com/resolve-os-image: replace-me
    workers:
      machineDeployments:
        - class: node-pool
          name: base-np
          replicas: 1
          metadata:
            annotations:
              run.tanzu.vmware.com/resolve-os-image: replace-me
          variables:
            overrides:
              - name: vmClass
                value: best-effort-large
              - name: volumes
                value:
                  - name: vol-sq33
                    mountPath: /var/lib/containerd
                    storageClass: vsan-default-storage-policy
                    capacity: 20Gi
