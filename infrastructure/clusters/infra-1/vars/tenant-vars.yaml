# Generated by terraform/infra (local_file) — do not edit manually.
# Populated from Terraform state during `make apply-infra`.

apiVersion: v1
kind: ConfigMap
metadata:
  name: tenant-vars
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  argo_namespace: "infra-wxm32"
  # This tenant's human identity group, verbatim from tenants.yaml `group:` —
  # exactly the string a VCFA token carries. cluster-var-injector binds it
  # read-only via apps/base/tenant-users. Empty `group:` renders a subject
  # nobody can hold, so the binding stays valid and grants nothing.
  tenant_group: "gitops.platform:no-tenant-group"
